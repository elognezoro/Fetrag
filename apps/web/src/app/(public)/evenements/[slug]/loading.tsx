import { DetailPageSkeleton } from '@/components/public/skeletons'

/** Chargement d'une fiche événement : en-tête, description et encart d'inscription. */
export default function EventLoading() {
  return <DetailPageSkeleton />
}
