import { DetailPageSkeleton } from '@/components/public/skeletons'

/** Chargement d'une fiche service : en-tête, description et formulaire de demande. */
export default function ServiceLoading() {
  return <DetailPageSkeleton />
}
