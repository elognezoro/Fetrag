import { ListPageSkeleton } from '@/components/public/skeletons'

/** Chargement générique des pages publiques (en-tête, filtres, grille de cartes). */
export default function PublicLoading() {
  return <ListPageSkeleton />
}
