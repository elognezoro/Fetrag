/**
 * Charge l'augmentation de types Auth.js déclarée par @fetrag/auth (session.user.id, mfaVerified).
 * Le fichier .d.ts du package n'est pas importé par son index : cette référence l'inclut dans la compilation de l'app.
 */
/// <reference path="../../../../packages/auth/src/next-auth.d.ts" />

export {}
