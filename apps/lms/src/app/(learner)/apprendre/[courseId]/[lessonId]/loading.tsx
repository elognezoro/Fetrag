import { ReaderSkeleton } from '@/components/learner/loading-skeletons'

/** Chargement du lecteur pédagogique (sommaire + zone d'activité). */
export default function Loading() {
  return <ReaderSkeleton />
}
